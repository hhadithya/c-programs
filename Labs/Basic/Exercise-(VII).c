#include<stdio.h>

int main() {

    printf("int type size(bytes): %d\n", sizeof(int));
    printf("unsigned int type size(bytes): %d\n", sizeof(unsigned int));
    printf("float type size(bytes): %d\n", sizeof(float));
    printf("double type size(bytes): %d\n", sizeof(double));
    printf("char type size(bytes): %d\n", sizeof(char));

    return 0;
}